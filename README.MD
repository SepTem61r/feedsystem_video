/
feedsystem_video_go-main/                                                                                                                                                                                                                        
├── backend/                    # Go 后端                                                                                                                                                                                                        
│   ├── go.mod / go.sum         # Go 模块依赖                                                                                                                                                                                                    
│   ├── Dockerfile              # 多阶段构建（api / worker 两个 target）                                                                                                                                                                         
│   ├── cmd/
│   │   ├── main.go             # API 服务入口（端口 8080）
│   │   └── worker/main.go      # Worker 进程入口（消费 MQ、异步落库）
│   ├── configs/                # YAML 配置文件
│   │   ├── config.yaml         # 本地开发（MySQL:3306）
│   │   ├── config.compose-local.yaml  # 本地用 compose 依赖（MySQL:3307）
│   │   └── config.docker.yaml  # 全容器化部署
│   └── internal/               # 核心业务代码
│       ├── account/            # 账号模块（注册、登录、改名、修改密码）
│       ├── auth/               # JWT 令牌生成与校验
│       ├── config/             # 配置加载
│       ├── db/                 # 数据库连接 + GORM AutoMigrate
│       ├── feed/               # Feed 流（最新、热门、按关注）
│       ├── http/               # 路由注册（Gin router）
│       ├── middleware/
│       │   ├── jwt/            # JWT 认证中间件（强校验 / 软校验）
│       │   ├── rabbitmq/       # 消息队列生产者（点赞/评论/热度/关注/时间线）
│       │   ├── ratelimit/      # 限流中间件（按 IP / 按账号）
│       │   └── redis/          # Redis 缓存客户端 + ZSet 操作
│       ├── observability/      # pprof 性能分析
│       ├── social/             # 社交模块（关注/取关/粉丝/关注列表）
│       ├── video/              # 视频模块（视频CRUD、点赞、评论、热度缓存）
│       └── worker/             # MQ 消费者（点赞Worker、评论Worker、关注Worker、热度Worker、Outbox轮询器）
├── frontend/                   # 前端（Vite + React/Vue）
├── data/                       # 初始化数据 / SQL
├── test/                       # 测试文件
├── picture/                    # 图片资源
├── docker-compose.yml          # 一键启动 6 个容器（mysql, redis, rabbitmq, backend, worker, frontend）
├── start.sh                    # 启动脚本
└── feedsystem_video_go项目设计.md  # 详细设计文档

架构要点

┌───────────┬──────────────────────┬──────────────────────────────────────────────────┐
│    层     │         技术         │                       说明                       │
├───────────┼──────────────────────┼──────────────────────────────────────────────────┤
│ HTTP 框架 │ Gin                  │ RESTful API，路由按模块分组                      │
├───────────┼──────────────────────┼──────────────────────────────────────────────────┤
│ ORM       │ GORM                 │ AutoMigrate 自动建表                             │
├───────────┼──────────────────────┼──────────────────────────────────────────────────┤
│ 数据库    │ MySQL 8.0            │ 持久化存储                                       │
├───────────┼──────────────────────┼──────────────────────────────────────────────────┤
│ 缓存      │ Redis 7              │ 登录状态、热度排行(ZSet)、限流计数               │
├───────────┼──────────────────────┼──────────────────────────────────────────────────┤
│ 消息队列  │ RabbitMQ             │ API 进程写 MQ，Worker 进程异步消费落库           │
├───────────┼──────────────────────┼──────────────────────────────────────────────────┤
├── frontend/                   # 前端（Vite + React/Vue）
├── data/                       # 初始化数据 / SQL
├── test/                       # 测试文件
├── picture/                    # 图片资源
├── docker-compose.yml          # 一键启动 6 个容器（mysql, redis, rabbitmq, backend, worker, frontend）
├── start.sh                    # 启动脚本
└── feedsystem_video_go项目设计.md  # 详细设计文档

架构要点

┌───────────┬──────────────────────┬──────────────────────────────────────────────────┐
│    层     │         技术         │                       说明                       │
├───────────┼──────────────────────┼──────────────────────────────────────────────────┤
│ HTTP 框架 │ Gin                  │ RESTful API，路由按模块分组                      │
├───────────┼──────────────────────┼──────────────────────────────────────────────────┤
│ ORM       │ GORM                 │ AutoMigrate 自动建表                             │
├───────────┼──────────────────────┼──────────────────────────────────────────────────┤
│ 数据库    │ MySQL 8.0            │ 持久化存储                                       │
├───────────┼──────────────────────┼──────────────────────────────────────────────────┤
│ 缓存      │ Redis 7              │ 登录状态、热度排行(ZSet)、限流计数               │
├───────────┼──────────────────────┼──────────────────────────────────────────────────┤
│ 消息队列  │ RabbitMQ             │ API 进程写 MQ，Worker 进程异步消费落库           │
├───────────┼──────────────────────┼──────────────────────────────────────────────────┤
│ 认证      │ JWT                  │ 硬校验（必须登录）与软校验（可选登录）两种中间件 │
├───────────┼──────────────────────┼──────────────────────────────────────────────────┤
│ 限流      │ Token Bucket (Redis) │ 登录/注册按 IP，写操作按账号                     │
└───────────┴──────────────────────┴──────────────────────────────────────────────────┘

部署方式

- Docker Compose 全容器化：docker compose up -d --build，前端 5173、API 8080、RabbitMQ 管理台 15672
- 本地混合开发：compose 只启 MySQL/Redis/RabbitMQ，Go 和前端各自本地启动

核心数据流

API 进程处理请求后，将写操作（点赞、评论、关注、视频发布）的消息投递到 RabbitMQ，Worker 进程异步消费这些消息，执行数据库写入和 Redis 热度缓存更新，实现读写分离和削峰。